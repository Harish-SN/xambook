apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-xambook-gateway-grafana
  namespace: monitoring
spec:
  from:
    - {group: gateway.networking.k8s.io, kind: HTTPRoute, namespace: monitoring}
  to:
    - {group: "", kind: Service}
